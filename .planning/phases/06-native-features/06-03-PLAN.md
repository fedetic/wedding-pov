---
phase: 06-native-features
plan: 03
type: execute
wave: 3
depends_on:
  - 06-02
files_modified:
  - package.json
  - package-lock.json
  - ios/App/App/Info.plist
  - src/components/native/BiometricLockScreen.tsx
  - src/app/(organizer)/dashboard/settings/page.tsx
  - src/app/(organizer)/dashboard/page.tsx
  - ios/
  - android/
autonomous: true
requirements:
  - NATIVE-02

must_haves:
  truths:
    - "Organizer can navigate from /dashboard to /settings via a 'Settings' link in the dashboard header"
    - "On /settings, on native the organizer sees a 'Face ID / Touch ID' toggle row with description 'Require biometric authentication on app launch'"
    - "On web /settings, the page renders but the biometric toggle row does not appear"
    - "Enabling the toggle persists biometricEnabled=true to @capacitor/preferences and immediately confirms by triggering the biometric prompt"
    - "If biometric prompt fails or biometrics are unavailable, the toggle does NOT enable"
    - "BiometricLockScreen component exists, exports a default check function that returns 'unlocked' | 'fallback' | 'skip' based on Preferences + BiometricAuth.checkBiometry + BiometricAuth.authenticate"
    - "Info.plist contains NSFaceIDUsageDescription key with the user-facing string from UI-SPEC"
  artifacts:
    - path: "package.json"
      provides: "@aparajita/capacitor-biometric-auth ^10.0.0 in dependencies"
      contains: "@aparajita/capacitor-biometric-auth"
    - path: "ios/App/App/Info.plist"
      provides: "NSFaceIDUsageDescription privacy string (required before any Face ID API call)"
      contains: "NSFaceIDUsageDescription"
    - path: "src/components/native/BiometricLockScreen.tsx"
      provides: "Fullscreen overlay component + checkAndLock() helper"
      contains: "BiometricAuth"
    - path: "src/app/(organizer)/dashboard/settings/page.tsx"
      provides: "Settings page with biometric toggle (native-only row)"
      contains: "biometricEnabled"
    - path: "src/app/(organizer)/dashboard/page.tsx"
      provides: "Existing dashboard with new 'Settings' nav link"
      contains: "/dashboard/settings"
  key_links:
    - from: "Settings page toggle"
      to: "@capacitor/preferences Preferences.set({ key: 'biometricEnabled' })"
      via: "onChange handler after BiometricAuth.authenticate succeeds"
      pattern: "Preferences\\.set\\(\\{[^}]*biometricEnabled"
    - from: "BiometricLockScreen.checkAndLock()"
      to: "BiometricAuth.checkBiometry + BiometricAuth.authenticate"
      via: "isAvailable check then authenticate"
      pattern: "BiometricAuth\\.authenticate"
---

<objective>
Implement NATIVE-02: organizer can enable Face ID / Touch ID in app settings; subsequent app cold launches require biometric confirmation before showing the dashboard.

Scope of this plan:
- Install `@aparajita/capacitor-biometric-auth` (the correct package per RESEARCH.md; `@capacitor-community/biometric-auth` does NOT exist)
- Add `NSFaceIDUsageDescription` to iOS Info.plist (mandatory before any Face ID API call — Pitfall 5)
- Create `/dashboard/settings` page with biometric toggle row (toggle row is native-only; page is web-accessible)
- Create `BiometricLockScreen.tsx` component + `checkAndLock()` helper (mounted in layout in plan 04)
- Add a "Settings" link in the dashboard header
- Run `npx cap sync` to wire the new plugin into ios/ and android/

NOT in scope: mounting `BiometricLockScreen` in `layout.tsx` (plan 04 owns layout wiring); device-level verification of biometric flow (plan 04 checkpoint).
</objective>

<execution_context>
@/Users/304390/Documents/dev/github/wedding-pov/.claude/get-shit-done/workflows/execute-plan.md
@/Users/304390/Documents/dev/github/wedding-pov/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-native-features/06-CONTEXT.md
@.planning/phases/06-native-features/06-RESEARCH.md
@.planning/phases/06-native-features/06-UI-SPEC.md
@.planning/phases/06-native-features/06-02-SUMMARY.md
@src/components/ConnectDriveButton.tsx
@src/app/(organizer)/dashboard/page.tsx

<interfaces>
<!-- @aparajita/capacitor-biometric-auth v10 (verified in RESEARCH.md Standard Stack + Pattern 2): -->
- `import { BiometricAuth, BiometryError } from "@aparajita/capacitor-biometric-auth";`
- `BiometricAuth.checkBiometry(): Promise<{ isAvailable: boolean; strongBiometryIsAvailable: boolean; biometryType: number; biometryTypes: number[]; reason: string; code: string }>` — use ONLY `isAvailable` / `strongBiometryIsAvailable` for capability check; Android `biometryType` is unreliable (RESEARCH.md Anti-Patterns)
- `BiometricAuth.authenticate(options?: { reason?: string; cancelTitle?: string; allowDeviceCredential?: boolean; iosFallbackTitle?: string; androidTitle?: string; androidSubtitle?: string; androidConfirmationRequired?: boolean }): Promise<void>` — resolves on success, throws BiometryError on failure (cancel, lockout, no enrollment, etc.)
- `BiometryError` instance has `.code` property (string error code)

<!-- @capacitor/preferences API (already installed): -->
- `import { Preferences } from "@capacitor/preferences";`
- `Preferences.set({ key: string, value: string }): Promise<void>`
- `Preferences.get({ key: string }): Promise<{ value: string | null }>`
- `Preferences.remove({ key: string }): Promise<void>`

<!-- Existing dashboard page structure (from current /Users/304390/Documents/dev/github/wedding-pov/src/app/(organizer)/dashboard/page.tsx, verified): -->
- Server Component (default export `async function DashboardPage`)
- Imports SignOutButton, DisconnectDriveButton, ConnectDriveButton, EventList, EventCreatedBanner
- Header structure: `<div className="flex justify-between items-baseline mb-2"><h1>Welcome, {name}</h1><SignOutButton /></div>` — the Settings link goes in this header area
- Layout: `<main className="min-h-screen px-4 py-6 sm:p-8 max-w-2xl mx-auto">`

<!-- Settings page (NEW): -->
- Must be a Server Component for the page itself (auth check + redirect), with a Client Component for the toggle interaction
- Route: `src/app/(organizer)/dashboard/settings/page.tsx`
- The `(organizer)` route group is the existing dashboard group; auth middleware (proxy.ts in this Next 16 version) protects everything under it

<!-- Info.plist current state (verified): -->
- Existing keys end with WKAppBoundDomains array and CFBundleURLTypes array
- The closing structure is `...</array></dict></plist>` — new key goes before the final `</dict>`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @aparajita/capacitor-biometric-auth + Info.plist + cap sync</name>
  <files>package.json, package-lock.json, ios/App/App/Info.plist, ios/, android/</files>
  <read_first>
    - package.json (confirm no biometric package present; confirm @capacitor/core version)
    - .planning/phases/06-native-features/06-RESEARCH.md (Standard Stack — exact version ^10.0.0; Pitfall 1 — do NOT install @capacitor-community/biometric-auth; Pitfall 5 — NSFaceIDUsageDescription mandatory; Pattern 2 — biometric usage)
    - .planning/phases/06-native-features/06-UI-SPEC.md (Copywriting Contract — the iOS Face ID privacy string copy)
    - ios/App/App/Info.plist (current file — verify closing `</dict></plist>` structure; new key inserted before final `</dict>`)
  </read_first>
  <action>
    Run `npm install @aparajita/capacitor-biometric-auth@^10.0.0` from project root. Goes into `dependencies` (not devDependencies).

    Do NOT install `@capacitor-community/biometric-auth` — RESEARCH.md Pitfall 1 confirms it returns HTTP 404 from the npm registry. If the orchestrator hands you that name, replace it with `@aparajita/capacitor-biometric-auth`.

    Verify install: `npm ls @aparajita/capacitor-biometric-auth` must show `@aparajita/capacitor-biometric-auth@10.x` with no UNMET PEER errors against `@capacitor/core@^8`.

    Modify `ios/App/App/Info.plist` to add `NSFaceIDUsageDescription`. Insert the following two lines BEFORE the closing `</dict>` (i.e., after the closing `</array>` of `CFBundleURLTypes`):

    ```
    <key>NSFaceIDUsageDescription</key>
    <string>Wedding POV uses Face ID to protect your account.</string>
    ```

    The exact string MUST match what App Store reviewers will see; do NOT shorten or modify the copy. (This string is shown by iOS the first time Face ID is invoked.)

    Run `npx cap sync` after Info.plist edit and package install. This will:
    - Copy biometric plugin native code into `ios/App/Pods/` and `android/app/`
    - Sync Info.plist into the Xcode project view
    - Potentially rewrite portions of `ios/App/App.xcodeproj/project.pbxproj` — VERIFY that the `CODE_SIGN_ENTITLEMENTS = App/App.entitlements;` lines added in plan 02 are still present. If they were removed by cap sync, re-add them by repeating plan 02 task 2's pbxproj edit.

    Do NOT modify `App.entitlements` in this task.
  </action>
  <verify>
    <automated>npm ls @aparajita/capacitor-biometric-auth 2>&amp;1 | grep -q '@aparajita/capacitor-biometric-auth@10' &amp;&amp; grep -c 'NSFaceIDUsageDescription' ios/App/App/Info.plist | grep -q '^[[:space:]]*1$' &amp;&amp; grep -c 'CODE_SIGN_ENTITLEMENTS = App/App.entitlements' ios/App/App.xcodeproj/project.pbxproj | grep -qv '^[[:space:]]*0$'</automated>
  </verify>
  <acceptance_criteria>
    - `grep '"@aparajita/capacitor-biometric-auth"' package.json` shows a line containing `"^10.`
    - `npm ls @aparajita/capacitor-biometric-auth` exits 0 and prints `@aparajita/capacitor-biometric-auth@10.`
    - `npm ls @capacitor-community/biometric-auth` exits non-zero (package must NOT be installed)
    - `grep -c '<key>NSFaceIDUsageDescription</key>' ios/App/App/Info.plist` outputs 1
    - `grep -c 'Wedding POV uses Face ID to protect your account.' ios/App/App/Info.plist` outputs 1
    - `plutil -lint ios/App/App/Info.plist` prints "OK"
    - `grep -c 'CODE_SIGN_ENTITLEMENTS = App/App.entitlements' ios/App/App.xcodeproj/project.pbxproj` outputs at least 2 (plan 02 wiring preserved through cap sync)
    - `ios/App/Podfile.lock` contains `AparajitaCapacitorBiometricAuth` (or the plugin's exact pod name as published — confirm by running `grep -i biometric ios/App/Podfile.lock`)
    - `git status` shows expected modifications in `ios/` and `android/`
  </acceptance_criteria>
  <done>Biometric package installed at exact major version 10; NSFaceIDUsageDescription present in Info.plist with the exact UI-SPEC copy; Info.plist is valid plist; cap sync completed; plan 02's entitlement wiring survived cap sync (re-added if necessary).</done>
</task>

<task type="auto">
  <name>Task 2: Create BiometricLockScreen.tsx component (with checkAndLock helper and 3-failure fallback)</name>
  <files>src/components/native/BiometricLockScreen.tsx</files>
  <read_first>
    - .planning/phases/06-native-features/06-RESEARCH.md (Code Examples §Pattern 2 — biometric lock gate; also Anti-Patterns — do NOT use biometryType; use isAvailable/strongBiometryIsAvailable)
    - .planning/phases/06-native-features/06-UI-SPEC.md (Component Inventory §3 — Biometric Lock Screen styling and copy; Interaction States — 3-failure fallback; Accessibility — role=dialog aria-modal=true)
    - .planning/phases/06-native-features/06-CONTEXT.md (lines 24-30 — locked decisions: cold launch only, silent fallback, 3-failure threshold)
    - src/components/native/DeepLinkHandler.tsx (recently created — pattern for Capacitor.isNativePlatform() guard and native component style)
  </read_first>
  <action>
    Create `src/components/native/BiometricLockScreen.tsx`. EXACT content:

    ```
    "use client";

    import { useEffect, useState, useCallback } from "react";
    import { BiometricAuth, BiometryError } from "@aparajita/capacitor-biometric-auth";
    import { Preferences } from "@capacitor/preferences";
    import { Capacitor } from "@capacitor/core";

    type LockState = "checking" | "locked" | "unlocked" | "fallback" | "skip";

    /**
     * Reusable helper — runs the cold-launch lock check.
     * Returns:
     *   'skip'      — non-native platform OR biometricEnabled !== 'true' (user opted out)
     *   'fallback'  — biometrics unavailable on device OR user failed/cancelled the prompt
     *   'unlocked'  — user successfully authenticated
     *
     * Per CONTEXT.md decision: lock granularity is cold launch only — backgrounding
     * and resuming does NOT re-lock. This function should be invoked once when the
     * BiometricLockScreen component mounts (which happens once per cold start in layout).
     */
    export async function checkAndLock(): Promise<"unlocked" | "fallback" | "skip"> {
      if (!Capacitor.isNativePlatform()) return "skip";

      const { value } = await Preferences.get({ key: "biometricEnabled" });
      if (value !== "true") return "skip";

      try {
        const result = await BiometricAuth.checkBiometry();
        if (!result.isAvailable) return "fallback";
      } catch {
        return "fallback";
      }

      try {
        await BiometricAuth.authenticate({
          reason: "Unlock Wedding POV",
          allowDeviceCredential: false,
        });
        return "unlocked";
      } catch (error) {
        // BiometryError covers user-cancel, lockout, no-enrollment, etc.
        // Per CONTEXT.md: do not show error UI for unavailability — silent fallback.
        if (error instanceof BiometryError) {
          return "fallback";
        }
        return "fallback";
      }
    }

    /**
     * Cold-launch lock gate. Renders a fullscreen overlay while authenticating;
     * unmounts itself on success or fallback. Per UI-SPEC §3:
     *   - role=dialog aria-modal=true
     *   - bg-white fullscreen, centered icon + heading + body + button
     *   - "Use password instead" link appears after 3 consecutive failures
     *
     * Mount once at layout level (plan 04 owns layout.tsx wiring).
     */
    export function BiometricLockScreen() {
      const [state, setState] = useState<LockState>("checking");
      const [failureCount, setFailureCount] = useState(0);

      const attempt = useCallback(async () => {
        if (!Capacitor.isNativePlatform()) {
          setState("skip");
          return;
        }

        const { value } = await Preferences.get({ key: "biometricEnabled" });
        if (value !== "true") {
          setState("skip");
          return;
        }

        try {
          const cap = await BiometricAuth.checkBiometry();
          if (!cap.isAvailable) {
            setState("fallback");
            return;
          }
        } catch {
          setState("fallback");
          return;
        }

        setState("locked");
        try {
          await BiometricAuth.authenticate({
            reason: "Unlock Wedding POV",
            allowDeviceCredential: false,
          });
          setState("unlocked");
        } catch {
          setFailureCount((c) => c + 1);
          setState("locked");
        }
      }, []);

      useEffect(() => {
        attempt();
      }, [attempt]);

      if (state === "checking" || state === "skip" || state === "unlocked" || state === "fallback") {
        return null;
      }

      // state === "locked" — show overlay
      return (
        <div
          role="dialog"
          aria-modal="true"
          aria-label="Biometric authentication required"
          className="fixed inset-0 bg-white flex flex-col items-center justify-center gap-6 z-50"
        >
          <svg
            width="24"
            height="24"
            viewBox="0 0 24 24"
            fill="none"
            stroke="#171717"
            strokeWidth="1.5"
            aria-hidden="true"
          >
            <rect x="5" y="11" width="14" height="9" rx="2" />
            <path d="M8 11V8a4 4 0 0 1 8 0v3" />
          </svg>
          <h1 className="text-2xl font-semibold text-center">Unlock Wedding POV</h1>
          <p className="text-sm font-normal text-gray-500 text-center max-w-[280px]">
            Use Face ID or Touch ID to continue
          </p>
          <button
            type="button"
            onClick={attempt}
            autoFocus
            className="bg-black text-white text-sm font-semibold py-2 px-6 rounded hover:bg-gray-800"
          >
            Authenticate
          </button>
          {failureCount >= 3 && (
            <button
              type="button"
              onClick={() => setState("fallback")}
              className="text-sm font-normal text-gray-500 underline hover:text-black"
            >
              Use password instead
            </button>
          )}
        </div>
      );
    }
    ```

    Implementation rules (do not deviate):
    - Lock granularity is cold-launch only: this component runs `attempt()` exactly once on mount (the useEffect with no router/state deps). No app state listener re-locks on backgrounding — per CONTEXT.md locked decision.
    - "Use password instead" appears at failureCount >= 3 — exact threshold from CONTEXT.md.
    - Fallback link DOES NOT log the user out — it dismisses the overlay (setState("fallback") returns null) so the existing session continues.
    - Use `isAvailable` only — do NOT use `biometryType` for capability decisions (Anti-Pattern in RESEARCH.md).
    - The `reason: "Unlock Wedding POV"` string is shown by the OS in the iOS Face ID/Touch ID prompt — keep this exact copy.
    - Do NOT add a `Sign out` button or anywhere to navigate to login from the lock screen — per CONTEXT.md, lock failure does not auto-logout.

    Do NOT mount this component in any layout/page in this plan. Plan 04 wires it into layout.tsx.
  </action>
  <verify>
    <automated>test -f src/components/native/BiometricLockScreen.tsx &amp;&amp; npx tsc --noEmit 2>&amp;1 | grep -E 'BiometricLockScreen\.tsx' | wc -l | grep -q '^[[:space:]]*0$' &amp;&amp; grep -c 'BiometricAuth.authenticate' src/components/native/BiometricLockScreen.tsx | grep -qv '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/components/native/BiometricLockScreen.tsx` exits 0
    - `grep -c '"use client"' src/components/native/BiometricLockScreen.tsx` outputs 1
    - `grep -c 'export async function checkAndLock' src/components/native/BiometricLockScreen.tsx` outputs 1
    - `grep -c 'export function BiometricLockScreen' src/components/native/BiometricLockScreen.tsx` outputs 1
    - `grep -c '"@aparajita/capacitor-biometric-auth"' src/components/native/BiometricLockScreen.tsx` outputs 1
    - `grep -c 'BiometricAuth.checkBiometry' src/components/native/BiometricLockScreen.tsx` outputs 2 (helper + component)
    - `grep -c 'BiometricAuth.authenticate' src/components/native/BiometricLockScreen.tsx` outputs 2
    - `grep -c 'biometricEnabled' src/components/native/BiometricLockScreen.tsx` outputs 2 (helper + component each read it)
    - `grep -c 'failureCount >= 3' src/components/native/BiometricLockScreen.tsx` outputs 1 (the 3-failure threshold per CONTEXT.md)
    - `grep -c '>Use password instead<' src/components/native/BiometricLockScreen.tsx` outputs 1 (exact UI-SPEC copy)
    - `grep -c '>Unlock Wedding POV<' src/components/native/BiometricLockScreen.tsx` outputs 1
    - `grep -c '>Authenticate<' src/components/native/BiometricLockScreen.tsx` outputs 1
    - `grep -c 'role="dialog"' src/components/native/BiometricLockScreen.tsx` outputs 1 (accessibility)
    - `grep -c 'aria-modal="true"' src/components/native/BiometricLockScreen.tsx` outputs 1
    - `grep -c 'biometryType' src/components/native/BiometricLockScreen.tsx` outputs 0 (anti-pattern check)
    - `npx tsc --noEmit` shows zero errors in this file
  </acceptance_criteria>
  <done>BiometricLockScreen.tsx exports both the React component and the checkAndLock() helper. Component renders the fullscreen overlay with exact UI-SPEC copy and styling, supports the 3-failure fallback, uses isAvailable (not biometryType), and accessibility attrs (role=dialog, aria-modal). TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 3: Create /dashboard/settings page with biometric toggle + add Settings nav link to dashboard</name>
  <files>src/app/(organizer)/dashboard/settings/page.tsx, src/app/(organizer)/dashboard/page.tsx</files>
  <read_first>
    - src/app/(organizer)/dashboard/page.tsx (current dashboard — note Server Component pattern, auth.api.getSession, redirect, and the header structure at the `<div className="flex justify-between items-baseline mb-2">` block where Settings link goes)
    - src/components/SignOutButton.tsx (any existing client button pattern)
    - .planning/phases/06-native-features/06-UI-SPEC.md (Component Inventory §2 — Settings page layout, toggle markup, exact classes; Copywriting Contract — every string; Accessibility — toggle role=switch aria-checked)
    - .planning/phases/06-native-features/06-RESEARCH.md (Architecture: Preferences for biometric flag persistence)
  </read_first>
  <action>
    **Step A — Create the settings page.**

    Create directory `src/app/(organizer)/dashboard/settings/` and write `src/app/(organizer)/dashboard/settings/page.tsx` as a Server Component that delegates the interactive toggle to a Client Component child. EXACT content:

    ```
    import { auth } from "@/lib/auth";
    import { headers } from "next/headers";
    import { redirect } from "next/navigation";
    import Link from "next/link";
    import { BiometricToggle } from "./BiometricToggle";

    export const metadata = { title: "Settings — Wedding POV" };

    export default async function SettingsPage() {
      const session = await auth.api.getSession({ headers: await headers() });
      if (!session) redirect("/login");

      return (
        <main className="min-h-screen px-4 py-6 sm:p-8 max-w-2xl mx-auto">
          <Link
            href="/dashboard"
            className="text-sm font-normal text-gray-500 hover:text-black"
          >
            ← Dashboard
          </Link>
          <h1 className="text-2xl font-semibold mt-4 mb-8">Settings</h1>

          <section className="border border-gray-200 rounded-lg p-6">
            <BiometricToggle />
          </section>
        </main>
      );
    }
    ```

    Create `src/app/(organizer)/dashboard/settings/BiometricToggle.tsx` as a Client Component. EXACT content:

    ```
    "use client";

    import { useEffect, useState } from "react";
    import { Capacitor } from "@capacitor/core";
    import { Preferences } from "@capacitor/preferences";
    import { BiometricAuth } from "@aparajita/capacitor-biometric-auth";

    export function BiometricToggle() {
      const [isNative, setIsNative] = useState(false);
      const [enabled, setEnabled] = useState(false);
      const [busy, setBusy] = useState(false);

      useEffect(() => {
        if (!Capacitor.isNativePlatform()) return;
        setIsNative(true);
        Preferences.get({ key: "biometricEnabled" }).then(({ value }) => {
          setEnabled(value === "true");
        });
      }, []);

      async function toggle() {
        if (busy) return;
        setBusy(true);
        try {
          if (!enabled) {
            // Confirm capability + user can pass the prompt before enabling.
            const cap = await BiometricAuth.checkBiometry();
            if (!cap.isAvailable) {
              // Silent fallback per CONTEXT.md — toggle does not enable, no error UI.
              return;
            }
            try {
              await BiometricAuth.authenticate({
                reason: "Enable Face ID / Touch ID for Wedding POV",
                allowDeviceCredential: false,
              });
            } catch {
              return; // user cancelled or failed — toggle does not enable
            }
            await Preferences.set({ key: "biometricEnabled", value: "true" });
            setEnabled(true);
          } else {
            await Preferences.remove({ key: "biometricEnabled" });
            setEnabled(false);
          }
        } finally {
          setBusy(false);
        }
      }

      if (!isNative) return null; // toggle row is native-only per UI-SPEC §2

      return (
        <div className="flex items-center justify-between">
          <div className="flex flex-col gap-1">
            <span className="text-sm font-semibold">Face ID / Touch ID</span>
            <span className="text-xs font-normal text-gray-500">
              Require biometric authentication on app launch
            </span>
          </div>
          <button
            type="button"
            role="switch"
            aria-checked={enabled}
            aria-label="Face ID / Touch ID"
            onClick={toggle}
            disabled={busy}
            className={`relative w-[44px] h-[24px] rounded-full transition-colors duration-150 ${
              enabled ? "bg-black" : "bg-gray-200"
            } disabled:opacity-50`}
          >
            <span
              className={`absolute top-[2px] w-[20px] h-[20px] bg-white rounded-full shadow transition-transform duration-150 ${
                enabled ? "translate-x-[22px]" : "translate-x-[2px]"
              }`}
            />
          </button>
        </div>
      );
    }
    ```

    Rules:
    - Toggle row renders only when `isNative === true`. The page card (`<section>`) still renders on web — UI-SPEC §2 says page accessible on web, toggle row hidden.
    - Enabling the toggle MUST run `BiometricAuth.authenticate` BEFORE persisting `biometricEnabled = "true"` (UI-SPEC §Interaction States: "Enabling — Immediately trigger biometric prompt to confirm capability; if prompt fails/unavailable, do not enable").
    - Disabling clears the preference (no re-authentication required to disable).
    - No error UI for unavailability — silent fallback per CONTEXT.md.

    **Step B — Add Settings link to dashboard header.**

    Modify `src/app/(organizer)/dashboard/page.tsx`:
    - Add the import `import Link from "next/link";` at the top with the other imports.
    - In the header block `<div className="flex justify-between items-baseline mb-2">` (which currently contains `<h1>Welcome, {name}</h1>` and `<SignOutButton />`), change the right side from `<SignOutButton />` alone to a flex group containing a Settings Link and SignOutButton:
      ```
      <div className="flex items-center gap-4">
        <Link href="/dashboard/settings" className="text-sm font-normal text-gray-500 hover:text-black">
          Settings
        </Link>
        <SignOutButton />
      </div>
      ```
    - Do NOT modify any other part of dashboard/page.tsx (Drive section, EventList, etc.).
  </action>
  <verify>
    <automated>test -f src/app/\(organizer\)/dashboard/settings/page.tsx &amp;&amp; test -f src/app/\(organizer\)/dashboard/settings/BiometricToggle.tsx &amp;&amp; npx tsc --noEmit 2>&amp;1 | grep -E '(settings|dashboard)/(page|BiometricToggle)\.tsx' | wc -l | grep -q '^[[:space:]]*0$' &amp;&amp; npm run build 2>&amp;1 | tail -20 | grep -q 'Compiled successfully'</automated>
  </verify>
  <acceptance_criteria>
    - `test -f 'src/app/(organizer)/dashboard/settings/page.tsx'` exits 0
    - `test -f 'src/app/(organizer)/dashboard/settings/BiometricToggle.tsx'` exits 0
    - `grep -c 'Settings — Wedding POV' 'src/app/(organizer)/dashboard/settings/page.tsx'` outputs 1
    - `grep -c '← Dashboard' 'src/app/(organizer)/dashboard/settings/page.tsx'` outputs 1
    - `grep -c '>Settings<' 'src/app/(organizer)/dashboard/settings/page.tsx'` outputs 1 (page h1)
    - `grep -c 'redirect("/login")' 'src/app/(organizer)/dashboard/settings/page.tsx'` outputs 1 (auth guard parity with dashboard)
    - `grep -c '"use client"' 'src/app/(organizer)/dashboard/settings/BiometricToggle.tsx'` outputs 1
    - `grep -c 'role="switch"' 'src/app/(organizer)/dashboard/settings/BiometricToggle.tsx'` outputs 1
    - `grep -c 'aria-checked' 'src/app/(organizer)/dashboard/settings/BiometricToggle.tsx'` outputs 1
    - `grep -c 'BiometricAuth.authenticate' 'src/app/(organizer)/dashboard/settings/BiometricToggle.tsx'` outputs 1 (confirms prompt-before-persist behavior)
    - `grep -c "Preferences.set" 'src/app/(organizer)/dashboard/settings/BiometricToggle.tsx'` outputs 1
    - `grep -c "Preferences.remove" 'src/app/(organizer)/dashboard/settings/BiometricToggle.tsx'` outputs 1
    - `grep -c '>Face ID / Touch ID<' 'src/app/(organizer)/dashboard/settings/BiometricToggle.tsx'` outputs 1
    - `grep -c '>Require biometric authentication on app launch<' 'src/app/(organizer)/dashboard/settings/BiometricToggle.tsx'` outputs 1 (exact UI-SPEC copy)
    - `grep -c 'href="/dashboard/settings"' 'src/app/(organizer)/dashboard/page.tsx'` outputs 1
    - `grep -c '>Settings</Link>' 'src/app/(organizer)/dashboard/page.tsx'` outputs 1
    - `grep -c 'SignOutButton' 'src/app/(organizer)/dashboard/page.tsx'` outputs at least 2 (import + usage — unchanged)
    - `npx tsc --noEmit` shows zero errors
    - `npm run build` exits 0
    - `npm run lint` exits 0
  </acceptance_criteria>
  <done>Settings page exists at /dashboard/settings, server-side auth-gated, with BiometricToggle client child. Toggle is native-only, enables by first prompting biometric and persisting only on success, disables by removing the preference. Dashboard header has a new "Settings" Link beside SignOutButton. Production build succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Native biometric API → app | The OS-level BiometricPrompt / LAContext returns a boolean — no biometric data crosses the boundary; the app only learns "passed" or "failed". |
| @capacitor/preferences → app | Per-app sandboxed key-value store backed by iOS Keychain / Android SharedPreferences. |
| Lock gate → existing Better Auth session | The biometric prompt does NOT replace the auth cookie — it's an additional UI gate before the dashboard renders. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-08 | Spoofing | Biometric bypass attempt | mitigate | `allowDeviceCredential: false` blocks PIN/passcode fallback as biometric substitute; only Face ID / Touch ID enrolled fingerprints can pass; the 3-failure fallback opens a "use password" UX that requires the existing email/password (Better Auth) — not a biometric bypass |
| T-06-09 | Tampering | biometricEnabled flag in Preferences | accept | The flag controls only the LOCAL UI gate; toggling it via device root access does not grant API access (the Better Auth session cookie is the actual access control); worst case: an attacker with device access disables the local lock — but they would already have device access, which is outside our threat model for a consumer dashboard |
| T-06-10 | Repudiation | No audit trail of biometric attempts | accept | This is a local UX lock, not an audit-logged auth event; logging would require server round-trips defeating the offline-tolerant lock UX |
| T-06-11 | Denial of Service | Repeated biometric prompts lock out hardware | accept | iOS/Android handle hardware-level lockout natively (after ~5 failed attempts the OS disables biometrics until passcode is entered); the app's 3-failure threshold is independent and bypasses biometric ENTIRELY by showing "Use password instead" — gracefully reaches fallback before OS lockout |
| T-06-SC | Tampering | npm install (@aparajita/capacitor-biometric-auth) | mitigate | slopcheck [OK] per 06-RESEARCH.md Package Legitimacy Audit; 224 stars, 49 releases, 3-year-old reputable package; @capacitor-community/biometric-auth (the wrong name from CONTEXT.md) is explicitly NOT installed and Pitfall 1 documents this |
</threat_model>

<verification>
- TypeScript: `npx tsc --noEmit` exits 0
- Build: `npm run build` exits 0
- Lint: `npm run lint` exits 0
- Web (npm run dev): Visit `/dashboard/settings` — page renders with "Settings" heading and "← Dashboard" back link; biometric toggle row is NOT visible (because Capacitor.isNativePlatform() is false on web)
- Web: Visit `/dashboard` — Settings link is visible in the header
- Plist validity: `plutil -lint ios/App/App/Info.plist` prints OK
- Plan 02 entitlements preserved: `grep -c 'CODE_SIGN_ENTITLEMENTS = App/App.entitlements' ios/App/App.xcodeproj/project.pbxproj` outputs >= 2
</verification>

<success_criteria>
- NATIVE-02 core capability assembled: biometric plugin installed, NSFaceIDUsageDescription set, Settings page enables toggle gated by successful biometric prompt, BiometricLockScreen component ready to be mounted (plan 04)
- Web build of /dashboard/settings is functional (page accessible) with toggle row hidden
- Dashboard navigation includes "Settings" link
- Plan 02's entitlement wiring is preserved through cap sync
</success_criteria>

<output>
Create `.planning/phases/06-native-features/06-03-SUMMARY.md` following the template. Document: installed version of @aparajita/capacitor-biometric-auth, whether project.pbxproj entitlements wiring survived cap sync (and if re-applied), Info.plist insertion location, and any TypeScript type-import surprises with BiometryError.
</output>
