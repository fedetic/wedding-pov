---
phase: 06-native-features
plan: 04
type: execute
wave: 4
depends_on:
  - 06-02
  - 06-03
files_modified:
  - src/components/native/OfflineOverlay.tsx
  - src/components/native/NativeShell.tsx
  - src/app/layout.tsx
autonomous: true
requirements:
  - NATIVE-03
  - NATIVE-04

must_haves:
  truths:
    - "On native iOS/Android with no network: app shows a fullscreen offline overlay with 'No internet connection' heading and a 'Try again' button"
    - "On native: tapping 'Try again' re-checks connectivity via Network.getStatus(); if online, overlay unmounts; if still offline, button briefly shows 'Checking…' for 1s"
    - "On native: when connectivity is lost while app is open, the overlay appears automatically (reactive listener)"
    - "On native: when connectivity is restored, the overlay automatically unmounts (no manual retry required)"
    - "On native: Universal Link tap routes to /dashboard via DeepLinkHandler mounted in layout"
    - "On native: after cold launch with biometricEnabled=true, BiometricLockScreen blocks the dashboard until biometric passes or 3-failure fallback"
    - "On web: none of the three native components render — layout renders only {children} and any non-native content"
  artifacts:
    - path: "src/components/native/OfflineOverlay.tsx"
      provides: "Reactive fullscreen offline overlay using @capacitor/network"
      contains: "Network.addListener"
    - path: "src/components/native/NativeShell.tsx"
      provides: "Client wrapper mounting OfflineOverlay, DeepLinkHandler, BiometricLockScreen for native platforms"
      contains: "OfflineOverlay"
    - path: "src/app/layout.tsx"
      provides: "Root layout with NativeShell mounted alongside children"
      contains: "NativeShell"
  key_links:
    - from: "Network.addListener('networkStatusChange')"
      to: "OfflineOverlay isOffline state"
      via: "useEffect subscription in OfflineOverlay"
      pattern: "networkStatusChange"
    - from: "layout.tsx body"
      to: "NativeShell mount"
      via: "{children}"
      pattern: "<NativeShell"
    - from: "NativeShell"
      to: "OfflineOverlay + DeepLinkHandler + BiometricLockScreen"
      via: "single client component mounted once at root"
      pattern: "BiometricLockScreen"
---

<objective>
Implement NATIVE-04 (offline overlay) and wire the final layout integration for NATIVE-02 (BiometricLockScreen) and NATIVE-03 (DeepLinkHandler) created in plans 02 and 03. After this plan:
- `OfflineOverlay.tsx` exists and reactively shows when `@capacitor/network` reports `connected: false`
- `NativeShell.tsx` is a single client component that mounts all three native-only effects (OfflineOverlay visual, DeepLinkHandler listener, BiometricLockScreen lock gate)
- `src/app/layout.tsx` mounts `<NativeShell />` once at root

This is the final integration plan for Phase 6 — every requirement (NATIVE-01..04) is functionally complete after this plan.

Purpose: Layout-level mounting is intentionally consolidated into the final plan because (a) layout.tsx is the most-edited file in any large feature and concentrating its changes minimizes merge conflicts; (b) all three components must coexist at root and a single `NativeShell` wrapper keeps `layout.tsx` minimal; (c) the BiometricLockScreen mount must happen AFTER plan 03 ships the component file.

Output: OfflineOverlay component, NativeShell wrapper, layout.tsx update.
</objective>

<execution_context>
@/Users/304390/Documents/dev/github/wedding-pov/.claude/get-shit-done/workflows/execute-plan.md
@/Users/304390/Documents/dev/github/wedding-pov/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-native-features/06-CONTEXT.md
@.planning/phases/06-native-features/06-RESEARCH.md
@.planning/phases/06-native-features/06-UI-SPEC.md
@.planning/phases/06-native-features/06-02-SUMMARY.md
@.planning/phases/06-native-features/06-03-SUMMARY.md
@src/app/layout.tsx
@src/components/native/DeepLinkHandler.tsx
@src/components/native/BiometricLockScreen.tsx

<interfaces>
<!-- @capacitor/network API (already installed): -->
- `import { Network } from "@capacitor/network";`
- `Network.getStatus(): Promise<{ connected: boolean; connectionType: string }>`
- `Network.addListener('networkStatusChange', (status) => void): Promise<PluginListenerHandle>`
- Cleanup: `(await listenerPromise).remove()`

<!-- Existing layout.tsx (verified): -->
- Server Component (no "use client")
- Imports: `type Metadata`, `Geist, Geist_Mono from next/font/google`, `./globals.css`
- Default export `RootLayout({ children })`
- Body: `<body className="min-h-full flex flex-col">{children}</body>`
- We will ADD: `import { NativeShell } from "@/components/native/NativeShell";` and render `<NativeShell />` inside body, before `{children}`. NativeShell is itself a Client Component — Server layout can render Client children (Next.js App Router pattern).

<!-- Components created in earlier plans (must be in context): -->
- `import { DeepLinkHandler } from "@/components/native/DeepLinkHandler";` — from plan 02
- `import { BiometricLockScreen } from "@/components/native/BiometricLockScreen";` — from plan 03
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create OfflineOverlay.tsx with reactive @capacitor/network listener</name>
  <files>src/components/native/OfflineOverlay.tsx</files>
  <read_first>
    - .planning/phases/06-native-features/06-RESEARCH.md (Code Examples §Pattern 4 — Offline Overlay verbatim; Pitfall 6 — must guard with Capacitor.isNativePlatform())
    - .planning/phases/06-native-features/06-UI-SPEC.md (Component Inventory §4 — Offline Overlay styling, copy; Interaction States — Try again handler with "Checking…" 1s feedback; Accessibility — role=alert aria-live=assertive)
    - .planning/phases/06-native-features/06-CONTEXT.md (lines 41-46 — locked decisions: reactive overlay, retry checks connectivity and reloads WebView, overlay sits above WebView)
    - src/components/native/DeepLinkHandler.tsx (sibling component — same listener cleanup pattern: listenerPromise stored, .then(l => l.remove()).catch(() => {}))
  </read_first>
  <action>
    Create `src/components/native/OfflineOverlay.tsx`. EXACT content:

    ```
    "use client";

    import { useEffect, useState } from "react";
    import { Network } from "@capacitor/network";
    import { Capacitor } from "@capacitor/core";

    /**
     * Reactive fullscreen offline overlay (NATIVE-04).
     *
     * Behavior (per CONTEXT.md):
     *   - Mount listener on native only — web users have browser-level offline UX
     *   - Show overlay whenever Network reports connected: false
     *   - Auto-dismiss when connectivity returns (no manual retry required)
     *   - Try again button re-checks via Network.getStatus(); if still offline,
     *     shows "Checking…" feedback for 1000ms before resetting label
     *
     * The overlay sits above the WebView — does not navigate away — so the app
     * resumes in place when connectivity is restored (CONTEXT.md line 44).
     */
    export function OfflineOverlay() {
      const [isOffline, setIsOffline] = useState(false);
      const [checking, setChecking] = useState(false);

      useEffect(() => {
        if (!Capacitor.isNativePlatform()) return;

        let cancelled = false;
        Network.getStatus().then(({ connected }) => {
          if (!cancelled) setIsOffline(!connected);
        });

        const listenerPromise = Network.addListener("networkStatusChange", ({ connected }) => {
          setIsOffline(!connected);
        });

        return () => {
          cancelled = true;
          listenerPromise.then((l) => l.remove()).catch(() => {});
        };
      }, []);

      async function handleRetry() {
        if (checking) return;
        setChecking(true);
        try {
          const { connected } = await Network.getStatus();
          if (connected) {
            setIsOffline(false);
            return;
          }
          // Still offline — keep overlay; show "Checking…" feedback for 1s
          await new Promise((r) => setTimeout(r, 1000));
        } finally {
          setChecking(false);
        }
      }

      if (!isOffline) return null;

      return (
        <div
          role="alert"
          aria-live="assertive"
          className="fixed inset-0 bg-white flex flex-col items-center justify-center gap-4 z-50"
        >
          <svg
            width="24"
            height="24"
            viewBox="0 0 24 24"
            fill="none"
            stroke="#171717"
            strokeWidth="1.5"
            aria-hidden="true"
          >
            <path d="M1 9a16 16 0 0 1 22 0" />
            <path d="M5 13a10 10 0 0 1 14 0" />
            <path d="M9 17a4 4 0 0 1 6 0" />
            <line x1="3" y1="3" x2="21" y2="21" />
          </svg>
          <h1 className="text-lg font-semibold text-center">No internet connection</h1>
          <p className="text-sm font-normal text-gray-500 text-center max-w-[280px]">
            Check your connection and try again
          </p>
          <button
            type="button"
            onClick={handleRetry}
            autoFocus
            disabled={checking}
            className="bg-black text-white text-sm font-semibold py-2 px-6 rounded hover:bg-gray-800 disabled:opacity-50"
          >
            {checking ? "Checking…" : "Try again"}
          </button>
        </div>
      );
    }
    ```

    Rules (do not deviate):
    - The retry handler does NOT call `window.location.reload()` — UI-SPEC §Interaction States is explicit: "Network restored — Unmount overlay; do not reload the WebView automatically". The CONTEXT.md "retry button checks connectivity and reloads the WebView" was REVISED by the UI-SPEC (more recent, latest-wins for visual contract); the implementation here matches the UI-SPEC behavior. If a reload is desired in the future, it's an enhancement, not a Phase 6 requirement.
    - The reactive listener handles auto-dismiss when connectivity returns — that's why `Network.addListener` is the source of truth, not the retry button.
    - The overlay is fully opaque white (`bg-white`) — UI-SPEC explicit color contract; not translucent.
    - Heading copy is "No internet connection" (18px semibold per UI-SPEC §4 — `text-lg font-semibold`), NOT 24px display size — that size is reserved for the BiometricLockScreen heading per UI-SPEC.
    - Body copy is "Check your connection and try again" — exact UI-SPEC string.
    - Button label toggles between "Try again" and "Checking…" — exact UI-SPEC strings.
    - max-width on body text is 280px to keep line length readable.

    Do NOT mount this component anywhere in this task — task 2 creates NativeShell, task 3 mounts NativeShell in layout.
  </action>
  <verify>
    <automated>test -f src/components/native/OfflineOverlay.tsx &amp;&amp; npx tsc --noEmit 2>&amp;1 | grep -E 'OfflineOverlay\.tsx' | wc -l | grep -q '^[[:space:]]*0$'</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/components/native/OfflineOverlay.tsx` exits 0
    - `grep -c '"use client"' src/components/native/OfflineOverlay.tsx` outputs 1
    - `grep -c 'export function OfflineOverlay' src/components/native/OfflineOverlay.tsx` outputs 1
    - `grep -c '"@capacitor/network"' src/components/native/OfflineOverlay.tsx` outputs 1
    - `grep -c 'Network.getStatus' src/components/native/OfflineOverlay.tsx` outputs 2 (mount check + retry)
    - `grep -c 'networkStatusChange' src/components/native/OfflineOverlay.tsx` outputs 1
    - `grep -c 'Capacitor.isNativePlatform()' src/components/native/OfflineOverlay.tsx` outputs 1 (Pitfall 6 guard)
    - `grep -c '>No internet connection<' src/components/native/OfflineOverlay.tsx` outputs 1 (exact UI-SPEC copy)
    - `grep -c '>Check your connection and try again<' src/components/native/OfflineOverlay.tsx` outputs 1
    - `grep -c 'Try again' src/components/native/OfflineOverlay.tsx` outputs at least 1
    - `grep -c 'Checking…' src/components/native/OfflineOverlay.tsx` outputs 1 (exact ellipsis character — single … U+2026 per UI-SPEC, not three dots)
    - `grep -c 'role="alert"' src/components/native/OfflineOverlay.tsx` outputs 1
    - `grep -c 'aria-live="assertive"' src/components/native/OfflineOverlay.tsx` outputs 1
    - `grep -c 'window.location.reload' src/components/native/OfflineOverlay.tsx` outputs 0 (per UI-SPEC: do not auto-reload)
    - `npx tsc --noEmit` shows zero errors in this file
  </acceptance_criteria>
  <done>OfflineOverlay.tsx exists, exports a client component that subscribes to networkStatusChange, shows the overlay reactively when connected=false, auto-dismisses on reconnection, supports manual retry with "Checking…" feedback, and is gated by Capacitor.isNativePlatform(). All UI-SPEC copy is exact.</done>
</task>

<task type="auto">
  <name>Task 2: Create NativeShell.tsx wrapper that mounts all three native-only components</name>
  <files>src/components/native/NativeShell.tsx</files>
  <read_first>
    - src/components/native/OfflineOverlay.tsx (just created — verify the export name)
    - src/components/native/DeepLinkHandler.tsx (from plan 02 — verify export)
    - src/components/native/BiometricLockScreen.tsx (from plan 03 — verify export)
  </read_first>
  <action>
    Create `src/components/native/NativeShell.tsx`. EXACT content:

    ```
    "use client";

    import { OfflineOverlay } from "./OfflineOverlay";
    import { DeepLinkHandler } from "./DeepLinkHandler";
    import { BiometricLockScreen } from "./BiometricLockScreen";

    /**
     * Single client-side wrapper mounting all native-only effects.
     *
     * Mounted once at the root of app/layout.tsx. Each child component internally
     * gates its behavior with Capacitor.isNativePlatform() — so on web all three
     * render as no-ops (null DOM, no listeners attached).
     *
     * Layered z-index priority (when both overlays would show simultaneously):
     *   - OfflineOverlay (z-50)  — outermost, blocks all interaction
     *   - BiometricLockScreen (z-50) — also z-50; the offline overlay's later mount
     *     order in this component means it visually sits on top, which is the
     *     intended priority: a device that is offline AND requires biometric
     *     unlock should show the offline message first because biometric cannot
     *     succeed without the network anyway (BiometricAuth itself is offline-OK,
     *     but the dashboard it gates is network-dependent).
     *
     * DeepLinkHandler renders null — it is a listener-only component.
     */
    export function NativeShell() {
      return (
        <>
          <DeepLinkHandler />
          <BiometricLockScreen />
          <OfflineOverlay />
        </>
      );
    }
    ```

    Order matters: DeepLinkHandler first (listener-only, no visual), then BiometricLockScreen, then OfflineOverlay last so it stacks on top of the biometric overlay if both render simultaneously (offline supersedes biometric per the rationale above).

    Do NOT add server-side data fetching or session checks here — auth is handled by middleware (proxy.ts) on protected routes; the lock screen is a UI gate, not a security boundary.
  </action>
  <verify>
    <automated>test -f src/components/native/NativeShell.tsx &amp;&amp; npx tsc --noEmit 2>&amp;1 | grep -E 'NativeShell\.tsx' | wc -l | grep -q '^[[:space:]]*0$'</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/components/native/NativeShell.tsx` exits 0
    - `grep -c '"use client"' src/components/native/NativeShell.tsx` outputs 1
    - `grep -c 'export function NativeShell' src/components/native/NativeShell.tsx` outputs 1
    - `grep -c '<DeepLinkHandler' src/components/native/NativeShell.tsx` outputs 1
    - `grep -c '<BiometricLockScreen' src/components/native/NativeShell.tsx` outputs 1
    - `grep -c '<OfflineOverlay' src/components/native/NativeShell.tsx` outputs 1
    - Ordering check: in the file, `DeepLinkHandler` line number < `BiometricLockScreen` line number < `OfflineOverlay` line number (OfflineOverlay mounted last for correct stacking)
    - `npx tsc --noEmit` shows zero errors
  </acceptance_criteria>
  <done>NativeShell.tsx exports a client component that mounts DeepLinkHandler, BiometricLockScreen, and OfflineOverlay in fragment order. TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 3: Mount NativeShell in src/app/layout.tsx</name>
  <files>src/app/layout.tsx</files>
  <read_first>
    - src/app/layout.tsx (CURRENT file — verify it is a Server Component with no "use client", note imports at lines 1-3 and body structure at lines 25-32)
    - src/components/native/NativeShell.tsx (just created — confirm export)
  </read_first>
  <action>
    Modify `src/app/layout.tsx` with two changes ONLY:

    1. Add the import `import { NativeShell } from "@/components/native/NativeShell";` after the existing `import "./globals.css";` line (becomes line 4).

    2. Inside the existing `<body className="min-h-full flex flex-col">` element, add `<NativeShell />` as the FIRST child (before `{children}`). The body element will look exactly like:
       ```
       <body className="min-h-full flex flex-col">
         <NativeShell />
         {children}
       </body>
       ```

    DO NOT change:
    - The `"use client"` directive (layout.tsx is and remains a Server Component — Next.js App Router permits a Server Component layout to render a Client Component child)
    - The `metadata` export
    - The `Geist` and `Geist_Mono` font setup
    - The `<html>` element attributes or className
    - The `RootLayout` function signature

    Rationale: NativeShell is a Client Component ("use client" inside it); the Server Component layout can include Client Components as children without any wrapper. This is the official Next.js App Router pattern (verified in `node_modules/next/dist/docs/01-app/02-getting-started/03-layouts-and-pages.md` — AGENTS.md requires reading the local docs).
  </action>
  <verify>
    <automated>grep -c '<NativeShell' src/app/layout.tsx | grep -q '^[[:space:]]*1$' &amp;&amp; grep -c 'from "@/components/native/NativeShell"' src/app/layout.tsx | grep -q '^[[:space:]]*1$' &amp;&amp; npx tsc --noEmit 2>&amp;1 | grep -E 'layout\.tsx' | wc -l | grep -q '^[[:space:]]*0$' &amp;&amp; npm run build 2>&amp;1 | tail -30 | grep -q 'Compiled successfully'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'import { NativeShell } from "@/components/native/NativeShell"' src/app/layout.tsx` outputs 1
    - `grep -c '<NativeShell />' src/app/layout.tsx` outputs 1
    - `grep -c '"use client"' src/app/layout.tsx` outputs 0 (still a Server Component)
    - `grep -c 'Geist' src/app/layout.tsx` outputs at least 2 (Geist + Geist_Mono — fonts preserved)
    - `grep -c 'export const metadata' src/app/layout.tsx` outputs 1 (metadata preserved)
    - `grep -c 'export default function RootLayout' src/app/layout.tsx` outputs 1
    - `grep -B1 '{children}' src/app/layout.tsx | grep -q 'NativeShell'` — NativeShell renders BEFORE children
    - `npx tsc --noEmit` exits 0
    - `npm run build` exits 0 and prints "Compiled successfully" (or this version's equivalent)
    - `npm run lint` exits 0
    - Manual web check (after `npm run dev`): visit `http://localhost:3000` — no offline overlay, no biometric lock screen, no console errors. View source or React DevTools: `<NativeShell>` is in the tree but its three children render `null` because `Capacitor.isNativePlatform()` is false on the web.
  </acceptance_criteria>
  <done>layout.tsx renders <NativeShell /> as the first child of body. layout.tsx remains a Server Component. Production build and lint succeed. On web, all three native components render null (verified by absence of overlays at http://localhost:3000).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Network status from OS → app | The connected/disconnected signal originates in the OS network stack — trusted; no user input crosses. |
| NativeShell at root → all routes | The shell wraps every route; misbehavior here affects the entire app. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-12 | Denial of Service | OfflineOverlay listener leaked across navigations | mitigate | useEffect cleanup removes the listener; listenerPromise cleanup pattern verified (same as ConnectDriveButton.tsx and DeepLinkHandler.tsx); React StrictMode double-mount in dev is safe because cleanup runs between mounts |
| T-06-13 | Spoofing | Fake Network.getStatus() result via plugin hijack | accept | @capacitor/network was approved in Phase 5; the Capacitor JS bridge is sandboxed per-app; a hijacked plugin would require app reinstall under attacker control, which is outside the threat model |
| T-06-14 | Tampering | NativeShell rendered on web could leak native APIs | mitigate | Each child component internally guards with `Capacitor.isNativePlatform()` — on web they call no native APIs and render null; verified by `grep -c isNativePlatform` in OfflineOverlay (1), DeepLinkHandler (1), BiometricLockScreen (1+ in two places) |
| T-06-15 | Information Disclosure | Universal Link URL logged or echoed | mitigate | DeepLinkHandler routes to a FIXED `/dashboard` path and never echoes data.url to UI, console, or analytics; the incoming URL is read once and discarded |
| T-06-SC | Tampering | npm installs | accept | No npm installs in this plan; all packages used (@capacitor/network, @capacitor/core, @capacitor/app, @capacitor/preferences, @aparajita/capacitor-biometric-auth) were approved in Phase 5 (research) or plan 03 |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 across the project
- `npm run build` exits 0
- `npm run lint` exits 0
- Web smoke (`npm run dev` then visit http://localhost:3000): no console errors; no overlays visible; clicking around (login, register, public landing) works as before
- Web dashboard (after login) shows Settings link in header (from plan 03)
- Web `/dashboard/settings` page renders (toggle row hidden on web — plan 03)
- AASA endpoint serves valid JSON (plan 02)
- All 4 phase requirements (NATIVE-01..04) have functional code committed
</verification>

<success_criteria>
- NATIVE-04 functionally complete: offline overlay reactive on native, exact UI-SPEC copy
- NATIVE-03 functionally complete: AASA + assetlinks.json served, entitlements + intent filters configured, DeepLinkHandler mounted
- NATIVE-02 functionally complete: biometric package installed, NSFaceIDUsageDescription set, Settings page + toggle, BiometricLockScreen mounted
- NATIVE-01 functionally complete (from plan 01): Share button in QRModal
- Layout.tsx mounts the consolidated NativeShell exactly once at root
- All four requirement IDs mapped: NATIVE-01 (plan 01), NATIVE-02 (plans 03 + 04), NATIVE-03 (plans 02 + 04), NATIVE-04 (plan 04)
</success_criteria>

<output>
Create `.planning/phases/06-native-features/06-04-SUMMARY.md` following the template. Include: confirmation that layout.tsx is still a Server Component, the order of components inside NativeShell (DeepLinkHandler → BiometricLockScreen → OfflineOverlay), exact UI-SPEC copy compliance verification, and any deviations from UI-SPEC behavior (such as the retry handler not reloading WebView — document the rationale).
</output>
