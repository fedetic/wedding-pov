---
phase: 06-native-features
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/components/events/QRModal.tsx
  - ios/
  - android/
autonomous: true
requirements:
  - NATIVE-01

must_haves:
  truths:
    - "On native iOS/Android, organizer sees a 'Share QR code' button inside QRModal alongside the existing Download button"
    - "Tapping 'Share QR code' opens the native OS share sheet with the QR PNG file and guest URL text"
    - "On web, QRModal still shows only Download + Close buttons — Share button is hidden"
  artifacts:
    - path: "src/components/events/QRModal.tsx"
      provides: "Native share button + handleShare() function gated by Capacitor.isNativePlatform()"
      contains: "Share.share"
    - path: "package.json"
      provides: "@capacitor/share ^8.0.1 and @capacitor/filesystem ^8.1.2 in dependencies"
      contains: "@capacitor/share"
  key_links:
    - from: "QRModal.tsx handleShare()"
      to: "@capacitor/filesystem writeFile + getUri"
      via: "Cache directory PNG file"
      pattern: "Filesystem\\.writeFile.*Directory\\.Cache"
    - from: "QRModal.tsx handleShare()"
      to: "@capacitor/share Share.share()"
      via: "files: [uri] + text: guestUrl"
      pattern: "Share\\.share\\(\\{[^}]*files:"
---

<objective>
Implement NATIVE-01: organizer can share the event QR code PNG via the native OS share sheet on iOS and Android. The Share button lives inside the existing QRModal alongside the Download button, renders only on native platforms, and uses @capacitor/share + @capacitor/filesystem to convert the in-memory base64 PNG to a file:// URI before invoking the OS share sheet.

Purpose: First user-facing native capability in Phase 6 — the OS share sheet (Messages, AirDrop, WhatsApp, email, etc.) is the App Store 4.2 mitigation surface for "minimum functionality" rejection risk.

Output: QRModal.tsx with Share button + handler; @capacitor/share and @capacitor/filesystem installed and cap-synced into ios/ and android/.
</objective>

<execution_context>
@/Users/304390/Documents/dev/github/wedding-pov/.claude/get-shit-done/workflows/execute-plan.md
@/Users/304390/Documents/dev/github/wedding-pov/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-native-features/06-CONTEXT.md
@.planning/phases/06-native-features/06-RESEARCH.md
@.planning/phases/06-native-features/06-UI-SPEC.md
@src/components/events/QRModal.tsx
@src/components/ConnectDriveButton.tsx
@capacitor.config.ts

<interfaces>
<!-- Existing QRModal.tsx exports and state (verified from current file): -->
- `export function QRModal({ event, onClose }: QRModalProps)` — props `{ event: { name: string; slug: string }; onClose: () => void }`
- Internal state: `const [dataUrl, setDataUrl] = useState<string | null>(null)` — base64 PNG generated via `QRCode.toDataURL(url, { width: 256, margin: 2 })`
- Internal state: `const [guestUrl, setGuestUrl] = useState("")` — equals `${window.location.origin}/e/${event.slug}`
- Existing handler: `function handleDownload()` — pattern for new `handleShare` to follow
- Existing actions block markup (line 119): `<div className="flex flex-col gap-2">` containing Download button then Close button

<!-- New @capacitor/share API (verified from RESEARCH.md Pattern 1): -->
- `Share.share({ files: [string], text: string, dialogTitle?: string })` — `files` must be `file://` URIs (NOT data: URLs)

<!-- New @capacitor/filesystem API: -->
- `Filesystem.writeFile({ path: string, data: string, directory: Directory.Cache })` — `data` is RAW base64 (no `data:image/png;base64,` prefix); no `encoding` param for binary
- `Filesystem.getUri({ path: string, directory: Directory.Cache })` → `{ uri: string }` (file:// URI)
- `import { Filesystem, Directory } from "@capacitor/filesystem"`

<!-- Existing Capacitor.isNativePlatform() guard pattern from ConnectDriveButton.tsx: -->
- `import { Capacitor } from "@capacitor/core"`
- `if (Capacitor.isNativePlatform()) { ... native variant ... }` — sole gate; do NOT use user-agent sniffing or media queries
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @capacitor/share and @capacitor/filesystem and run cap sync</name>
  <files>package.json, package-lock.json, ios/, android/</files>
  <read_first>
    - package.json (to confirm current @capacitor/* versions are all ^8.x and not introduce a major-version mismatch)
    - .planning/phases/06-native-features/06-RESEARCH.md (Standard Stack section — exact versions and install command)
    - capacitor.config.ts (to confirm appId is com.weddingpov.app — cap sync will use this)
  </read_first>
  <action>
    Run `npm install @capacitor/share@^8.0.1 @capacitor/filesystem@^8.1.2` from the project root. Both packages must land in `dependencies` (not devDependencies) because they execute at runtime.

    After install, run `npx cap sync` to copy the new plugin native code into `ios/App/Pods/` and `android/app/`. This will modify generated files in `ios/App/App.xcworkspace`, `ios/App/Podfile.lock`, `android/app/capacitor.build.gradle`, `android/capacitor.settings.gradle`, and similar Capacitor-generated artifacts — all such modifications are expected and must be committed.

    Do NOT install `@capacitor-community/biometric-auth` — RESEARCH.md confirms it does not exist on npm (HTTP 404). The biometric plugin is installed in plan 03, not here.

    Verify install with `npm ls @capacitor/share @capacitor/filesystem` — both should show version 8.x with no UNMET PEER errors.
  </action>
  <verify>
    <automated>npm ls @capacitor/share @capacitor/filesystem 2>&amp;1 | grep -E '@capacitor/(share|filesystem)@8' | wc -l | grep -q '^[[:space:]]*2$'</automated>
  </verify>
  <acceptance_criteria>
    - `grep '"@capacitor/share"' package.json` outputs a line containing `"^8.`
    - `grep '"@capacitor/filesystem"' package.json` outputs a line containing `"^8.`
    - `npm ls @capacitor/share` exits 0 and prints `@capacitor/share@8.` (no UNMET PEER)
    - `npm ls @capacitor/filesystem` exits 0 and prints `@capacitor/filesystem@8.`
    - `ios/App/Podfile.lock` (after cap sync) contains the strings `CapacitorShare` and `CapacitorFilesystem`
    - `git status` shows modifications in `ios/` and `android/` subtrees (cap sync output)
  </acceptance_criteria>
  <done>Both packages are listed in package.json under dependencies with caret ranges ^8.0.1 and ^8.1.2 respectively; npx cap sync exits 0; iOS Podfile.lock contains both plugin pod names; Android capacitor.build.gradle references both plugin modules.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add Share button + handleShare() to QRModal.tsx (native-only)</name>
  <files>src/components/events/QRModal.tsx</files>
  <read_first>
    - src/components/events/QRModal.tsx (CURRENT full file — note existing imports at lines 1-3, dataUrl state at line 12, guestUrl state at line 16, handleDownload at line 43, actions block at line 119)
    - src/components/ConnectDriveButton.tsx (canonical Capacitor.isNativePlatform() guard pattern, lines 3 and 24)
    - .planning/phases/06-native-features/06-UI-SPEC.md (Component Inventory §1 — Share button styling and placement; Copywriting Contract — exact label "Share QR code"; Interaction States — share sheet errors are silent)
    - .planning/phases/06-native-features/06-RESEARCH.md (Code Examples §Complete QR Share Handler — verbatim implementation reference)
  </read_first>
  <action>
    Modify `src/components/events/QRModal.tsx` as follows:

    1. Add three imports at the top of the file (after the existing `import QRCode from "qrcode";` on line 3):
       ```
       import { Capacitor } from "@capacitor/core";
       import { Filesystem, Directory } from "@capacitor/filesystem";
       import { Share } from "@capacitor/share";
       ```

    2. Add a new state hook for native detection. Place it after the existing `const [guestUrl, setGuestUrl] = useState("");` on line 16:
       ```
       const [isNative, setIsNative] = useState(false);
       useEffect(() => {
         setIsNative(Capacitor.isNativePlatform());
       }, []);
       ```
       Rationale: `Capacitor.isNativePlatform()` is safe to call in client components but must be wrapped in useEffect to avoid SSR hydration mismatch (the QRModal is rendered from a server component context via EventRow).

    3. Add a new async function `handleShare` BELOW the existing `handleDownload` function (i.e., between line 51 closing brace of handleDownload and line 53 `async function handleCopy`). Exact implementation:
       ```
       async function handleShare() {
         if (!dataUrl) return;
         try {
           const base64 = dataUrl.split(",")[1];
           const fileName = `qr-${event.slug}.png`;
           await Filesystem.writeFile({
             path: fileName,
             data: base64,
             directory: Directory.Cache,
           });
           const { uri } = await Filesystem.getUri({
             path: fileName,
             directory: Directory.Cache,
           });
           await Share.share({
             files: [uri],
             text: guestUrl,
             dialogTitle: "Share QR code",
           });
         } catch {
           // OS-level share sheet errors (user cancel, etc.) are silent per UI-SPEC §Interaction States.
         }
       }
       ```
       Critical: Do NOT pass `dataUrl` directly to `Share.share({ url: dataUrl })` — RESEARCH.md Anti-Patterns confirms the share plugin rejects `data:` URLs and only accepts `file://` URIs.

    4. Add the Share button JSX inside the `<div className="flex flex-col gap-2">` actions block (line 119). Insert the new `<button>` BETWEEN the existing Download button (lines 120-127) and the Close button (lines 128-133). Conditionally render with `{isNative && ( ... )}`. Exact markup:
       ```
       {isNative && (
         <button
           type="button"
           onClick={handleShare}
           disabled={!dataUrl}
           className="w-full bg-black text-white text-sm font-semibold py-2 px-4 rounded hover:bg-gray-800 disabled:opacity-50 disabled:cursor-not-allowed"
         >
           Share QR code
         </button>
       )}
       ```

    Do NOT modify the Download button, Copy button, or Close button. Do NOT introduce a loading state for the share button — UI-SPEC §Interaction States explicitly states no loading state is needed (share sheet is synchronous from user perspective).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&amp;1 | grep -E 'QRModal\.tsx' | wc -l | grep -q '^[[:space:]]*0$' &amp;&amp; grep -c 'Share\.share' src/components/events/QRModal.tsx | grep -qv '^0$' &amp;&amp; grep -c 'Capacitor\.isNativePlatform' src/components/events/QRModal.tsx | grep -qv '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"@capacitor/share"' src/components/events/QRModal.tsx` outputs 1 (import present)
    - `grep -c '"@capacitor/filesystem"' src/components/events/QRModal.tsx` outputs 1
    - `grep -c '"@capacitor/core"' src/components/events/QRModal.tsx` outputs 1
    - `grep -c 'Capacitor.isNativePlatform' src/components/events/QRModal.tsx` outputs at least 1
    - `grep -c 'Share.share' src/components/events/QRModal.tsx` outputs 1
    - `grep -c 'Filesystem.writeFile' src/components/events/QRModal.tsx` outputs 1
    - `grep -c 'Filesystem.getUri' src/components/events/QRModal.tsx` outputs 1
    - `grep -c 'Directory.Cache' src/components/events/QRModal.tsx` outputs 2 (writeFile + getUri)
    - `grep -c '>Share QR code<' src/components/events/QRModal.tsx` outputs 1 (button label, exact match from UI-SPEC Copywriting Contract)
    - `grep -c 'handleDownload' src/components/events/QRModal.tsx` outputs at least 2 (existing function + onClick — unchanged)
    - `npx tsc --noEmit` shows zero errors in `src/components/events/QRModal.tsx`
    - `npm run build` exits 0 (Next.js production build succeeds)
  </acceptance_criteria>
  <done>QRModal.tsx contains the new Share button gated by `isNative`, handleShare() writes the QR PNG to Filesystem Cache and invokes Share.share() with files+text. TypeScript compiles cleanly. Next.js build succeeds. On web (Capacitor.isNativePlatform() === false), the Share button is not rendered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Native plugin → OS share sheet | Capacitor JS bridge passes file URI + text to the iOS UIActivityViewController / Android Intent.ACTION_SEND. The OS shows the picker; chosen target app receives the file. |
| App cache → OS share sheet → recipient app | Temporary PNG file in Directory.Cache is exposed to whichever app the user picks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | QR PNG containing guest URL stored in Directory.Cache | accept | Guest URL is intentionally public (printed on shareable QR); cache directory is sandboxed per-app on iOS/Android; the share action's whole purpose is to disclose this URL |
| T-06-02 | Tampering | Filename `qr-${slug}.png` derived from event.slug | mitigate | event.slug is generated server-side via nanoid (alphanumeric only per Phase 2); no path traversal risk in Filesystem.writeFile with Directory.Cache base; no user input on this path |
| T-06-SC | Tampering | npm installs (@capacitor/share, @capacitor/filesystem) | mitigate | Both packages slopcheck [OK] in 06-RESEARCH.md Package Legitimacy Audit; both are official Ionic plugins from github.com/ionic-team/capacitor-plugins; no [ASSUMED] or [SUS] packages installed in this plan |
</threat_model>

<verification>
- Web browser (npm run dev): QRModal opens, ONLY Download + Close buttons render (no Share button) — verifies isNative=false path
- TypeScript: `npx tsc --noEmit` exits 0
- Build: `npm run build` exits 0
- Lint: `npm run lint` exits 0
- Package legitimacy: both new packages are official `@capacitor/*` scoped packages; no third-party install
</verification>

<success_criteria>
- NATIVE-01 acceptance criterion met: organizer can tap Share QR code in QRModal on native and OS share sheet appears with QR PNG file + guest URL text
- QRModal on web is functionally unchanged (Download + Close only)
- @capacitor/share and @capacitor/filesystem are committed dependencies; cap sync output is committed
</success_criteria>

<output>
Create `.planning/phases/06-native-features/06-01-SUMMARY.md` following the template at `/Users/304390/Documents/dev/github/wedding-pov/.claude/get-shit-done/templates/summary.md`. Include the exact npm view versions installed, the QRModal.tsx import additions, and any unexpected cap sync output.
</output>
